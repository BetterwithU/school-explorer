# MEMORY — school-explorer (세션 시작 시 읽기)

## 진행 중 프로젝트: B = 중2 학교 탐험 레이스
- **상세 인수인계는 [games/B1/HANDOFF.md](../games/B1/HANDOFF.md) 를 먼저 읽을 것.** (작업 재개의 출발점)
- B는 A(초등 캠핑게임)와 **다른 게임**. 같은 레포에서 `core/`+`modes/`+`games/` 구조로 구축 중. **A는 안 건드림**(이주는 B 완성 후).
- 핵심 규칙: 2인1조 · N문항 주도 · N개 중 need개(기본 N-3) 먼저 풀고 복귀 우승 · 사진 유추 내비 · 힌트 총량 제한 · 단답형/고난도.
- 이동 모델: **② 실시간 혼잡도 동적배정 + ① 결정적 루트 폴백**, 오프라인-우선. **안정성이 사용자 1순위.**
- 만든 것: 출제.html(빌더) · modes/B/engine.js · core/sync.js · play.html · hq.html · stations.json(더미 N=8). 엔진 단위테스트 19/19 통과.
- 다음: ①오프라인-우선 강화 ②QR인쇄 ③core추출 ④부속물(참관안내문·조편성) ⑤검증·배포 ⑥A이주.

## 작업 원칙 (이 레포)
- 현재 문제 데이터는 **더미**(테스트용). 실제 문제는 사용자가 출제.html로 입력.
- 검증은 코드 읽기 + node 로직 테스트로. **헤드리스 스크린샷 금지, 임시 주입 파일 금지**(루트 CLAUDE.md 규칙).
- Git: BetterwithU 계정(SSH 별칭 `github-betterwithU`). push는 명시 허락 시.
